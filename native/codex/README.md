# Codex native distribution

The manifest under `native/codex/agentpack` is source-controlled. Run `npm run native:build` after compiling to resolve every catalog skill online and materialize the resulting point-in-time plugin under `dist/native/codex/agentpack`. The generated directory is not a source of truth; the online CLI is the normal install path.
