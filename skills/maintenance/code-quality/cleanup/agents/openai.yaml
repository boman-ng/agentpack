interface:
  display_name: "Cleanup"
  short_description: "Audit and prune accidental codebase complexity"
  default_prompt: "Use $cleanup to challenge non-trivial mechanisms against the simplest valid baseline, preserve verified outcomes and invariants, remove or narrow unproven complexity, verify the counterfactual, and report the net complexity change."
